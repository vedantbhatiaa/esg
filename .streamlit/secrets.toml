# Right now — your personal Azure/Copilot key
AZURE_OPENAI_KEY      = "paste-your-key-here"
AZURE_OPENAI_ENDPOINT = "https://YOUR-RESOURCE.openai.azure.com/openai/deployments/gpt-4o/chat/completions?api-version=2024-02-01"

# When company takes over — they just replace the two lines above
# with their enterprise Azure OpenAI credentials. Zero code changes.